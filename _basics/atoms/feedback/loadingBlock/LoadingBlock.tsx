import React, { FC, ReactElement } from 'react';

import { StyledLoadingBlock } from './LoadingBlock.styles';

const LoadingBlock: FC = (): ReactElement => <StyledLoadingBlock />;

export default LoadingBlock;
